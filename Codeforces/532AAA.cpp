#include<bits/stdc++.h>
#include<utility>
#include<algorithm>
#include<map>
#include<vector>
#include<cmath>
using namespace std;
typedef long long ll;

#define pb push_back
#define PI acos(-1.0)
#define MAX 10000007 //1e7+7
#define rep(X,Y) for (int (X) = 0;(X) < (Y);++(X))
#define rrep(X,Y) for (int (X) = (Y)-1;(X) >=0;--(X))
#define __FastIO  ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0)
int main(){
  __FastIO;
  ll n,d,r,i,l,m;
  ll arr[10005];
  string str;
  while(cin>>n>>d){
    cin>>string;
    map<string,ll>mp;
    for(i=1;i<str.length();i+d){
      for(int j=0;j<)
    }
/*    ll mx=0;
  for(i=0;i<n;i++){
    cin>>arr[i];
    mx=max(mx,arr[i]);
  }
  sort(arr,arr+n);
  ll cost=1000,t=0;
  for(i=0;i<=mx;i++){
    ll Cost=0;
    for(ll j=n-1;j>=0;j--){
      Cost=Cost+(abs(arr[j]-i));
    }
    if(Cost<cost){
      cost=Cost;
      t=i;
    }
  }
  cout<<cost<<" "<<i<<endl;
  */
  }
  return 0;
}
