#include<bits/stdc++.h>
#include<algorithm>
#include<cstdio>

using namespace std;
int main(){
    int n,arr[2000],p,sum,i,j,temp;
    while(scanf("%d",&n)!=EOF){
        for(i=0;i<n;i++){
            cin>>arr[i];
        }
        cin>>p;
        sort(arr,n);
    }
}
